server:
  port: 8095
spring:
  application:
    name: group-service
  cloud:
    config:
      uri: http://localhost:8081
      username: configUser
      password: configPassword
      name: group-service